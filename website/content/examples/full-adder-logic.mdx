---
title: "Logic gate: 1-bit full adder"
description: "Classic 1-bit full adder built from XOR/AND/OR gates. Sum and carry-out from three inputs (A, B, carry-in). Textbook digital logic example."
diagram: logic
industry: education
complexity: 2
standard: "IEEE 91"
---

The textbook 1-bit full adder — two XORs compute the sum bit, two ANDs
and an OR generate the carry-out. Every intro digital-logic course lands
on this circuit; every ALU is built from it.

<Playground initial={`logic "1-bit Full Adder"
input A, B, Cin
output Sum, Cout
s1 = XOR(A, B)
Sum = XOR(s1, Cin)
c1 = AND(A, B)
c2 = AND(s1, Cin)
Cout = OR(c1, c2)`} />

[Logic gate syntax →](/docs/logic)
