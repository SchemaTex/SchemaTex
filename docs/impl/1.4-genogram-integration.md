# 1.4 — Genogram Integration + End-to-End

*把 parser + symbols + layout + renderer 串起来，完成 Phase 1。*

> **Status**: Implemented
> **Priority**: Phase 1, Step 5 (final step)
> **Depends on**: 1.0, 1.1, 1.2, 1.3
> **Output**: Updated `src/diagrams/genogram/index.ts` + `src/core/api.ts`

---

## Goal

1. 在 `src/diagrams/genogram/index.ts` 中实现完整的 `DiagramPlugin`
2. 在 `src/core/api.ts` 中连接 auto-detection → plugin dispatch
3. End-to-end test: `render(text)` → valid SVG
4. 生成 example SVG 文件到 `examples/genogram/`
5. 确保 `npm run build` 产出正确的 dist/

---

## Implementation

### 1. Update genogram/index.ts

```ts
import { parseGenogram } from './parser';
import { layoutGenogram } from './layout';
import { renderGenogram } from './renderer';

export const genogram: DiagramPlugin = {
  type: 'genogram',
  detect: (text) => { /* check first line */ },
  parse: parseGenogram,
  layout: layoutGenogram,
  render: renderGenogram,
};
```

### 2. Update core/api.ts

```ts
import { genogram } from '../diagrams/genogram';
// future: import { ecomap } from '../diagrams/ecomap';

const plugins: DiagramPlugin[] = [genogram];

export function render(text: string, config?: SchematexConfig): string {
  const plugin = plugins.find(p => p.detect(text));
  if (!plugin) throw new Error('Could not detect diagram type');
  const ast = plugin.parse(text);
  const layoutConfig = { /* merge defaults with config */ };
  const layout = plugin.layout(ast, layoutConfig);
  const renderConfig = { /* merge defaults with config */ };
  return plugin.render(layout, renderConfig);
}
```

### 3. Generate Examples

生成至少 3 个 example SVG 到 `examples/genogram/`：
- `nuclear-family.svg` — 基础核心家庭
- `three-generation.svg` — 三代
- `medical-conditions.svg` — 带 condition fills

### 4. E2E Tests

```ts
describe('genogram end-to-end', () => {
  test('render() produces valid SVG from text', () => {
    const svg = render('genogram\n  john [male]\n  mary [female]\n  john -- mary');
    expect(svg).toContain('<svg');
    expect(svg).toContain('john');
  });

  test('auto-detects genogram type', () => {
    const ast = parse('genogram\n  a [male]');
    expect(ast.type).toBe('genogram');
  });

  test('all 6 standard test cases render without error', () => {
    for (const fixture of loadFixtures('genogram')) {
      expect(() => render(fixture)).not.toThrow();
    }
  });
});
```

### 5. Build Verification

```bash
npm run build
# Verify dist/ contains:
# - index.js, index.cjs, index.d.ts
# - diagrams/genogram/index.js, .cjs, .d.ts
ls -la dist/
```

---

## Definition of Done (Phase 1 Complete!)

- [x] `render(genogramText)` → valid SVG, end-to-end
- [x] `parse(genogramText)` → correct AST
- [x] 3+ example SVG files in `examples/genogram/`
- [x] All quality gates pass: typecheck, test, lint, build
- [ ] README example code actually works
- [ ] Visual check: open example SVGs in browser, verify correctness
- [x] npm package is publishable (`npm pack` produces valid tarball)
