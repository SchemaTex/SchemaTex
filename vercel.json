{
  "buildCommand": "npm run build && cd website && npm install --include=dev && npm run build",
  "outputDirectory": "website/.next",
  "installCommand": "npm install --legacy-peer-deps",
  "framework": "nextjs"
}
